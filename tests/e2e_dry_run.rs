//! e2e: dry-run 不写盘。
//!
//! 用户可见断言：跑完 `alluvium dry-run` 后，vault 目录不变（mtime / 内容均不变），
//! 但 stdout 上能看到蒸馏后的笔记预览。

#[test]
#[ignore = "scaffold v0.1: pending implementation"]
fn dry_run_does_not_write_vault() {
    // TODO
}
