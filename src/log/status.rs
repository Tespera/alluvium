//! Render `alluvium status` output from the audit log.

use anyhow::Result;

pub fn render_recent(_n: usize) -> Result<String> {
    anyhow::bail!("log::status::render_recent: not yet implemented (scaffold v0.1)")
}
