//! `alluvium consolidate` — manually trigger LLM rewrites of fragmented topic pages.
//!
//! Defends against the append-only-drift failure mode of Karpathy's wiki
//! pattern. v0.1 is manual; v0.2 adds scheduling.

use anyhow::Result;

pub async fn run() -> Result<()> {
    anyhow::bail!("alluvium consolidate: not yet implemented (scaffold v0.1)")
}
